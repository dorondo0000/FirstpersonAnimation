# Kept local, not shipped: the example packs and .bbmodel sources, the generator
# that produces them, the long-form notes, and the standalone scripts that the
# plugin has since absorbed.
examples/
docs/
tools/

node_modules/
*.log
*.zip
Thumbs.db
.DS_Store
